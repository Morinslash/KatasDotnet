namespace LeapYearSrc;

public static class LeapYear
{
    /// <summary>
    /// Returns true if the provided year is a leap year in the Gregorian calendar.
    /// </summary>
    public static bool IsLeapYear(int year)
    {
        // placeholder; tests will drive implementation
        return true;
    }
}
